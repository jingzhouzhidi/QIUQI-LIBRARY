筛选器标签：
政治 search_filters = {FOCUS_FILTER_POLITICAL}
科研 search_filters = {FOCUS_FILTER_RESEARCH}
工业 search_filters = {FOCUS_FILTER_INDUSTRY}
稳定度 search_filters = {FOCUS_FILTER_STABILITY}
战争支持度 search_filters = {FOCUS_FILTER_WAR_SUPPORT}
人力 search_filters = {FOCUS_FILTER_MANPOWER}
吞并 search_filters = {FOCUS_FILTER_ANNEXATION}

以下为国策树内容，国策树文件位置common/national_focus/xxx.txt
focus_tree = {
	id = mod_focus_tree
	country = {
		factor = 0
		modifier = {
			add = 10
			tag = GER
		}
	}
	default = no

	continuous_focus_position = { x = 1200 y = 100 }

	focus = {
		id = 单独国策ID（或名字）
		icon = 在.gfx文件里的图片名
		x = 5
		y = 0
		
		cost = 10

		ai_will_do = { 
			base = 1
			modifier = {
				条件
				add/factor = X
			}
		}
		available = {
			国策可点的条件
		}
		bypass = {
			跳过国策的条件
		}
		search_filters = { FOCUS_FILTER_MANPOWER }
		completion_reward = {
			效果
		}
		
	}

	focus = {
		id = 单独国策ID（或名字）
		icon = 在.gfx文件里的图片名
		relative_position_id = GER_guoce1
		prerequisite = { focus = 第一个前提国策ID focus = 第二个前提国策ID focus = 第N个前提国策ID } 先决国策，点完同一括弧内的任一国策即可点，虚线
		mutually_exclusive = { focus = 互斥国策ID }
		x = 以相对国策为原点的X轴横向坐标
		y = 以相对位国策为原点的Y轴纵向坐标

		cost = 国策需要的天数，一个值等于7天，可加小数点

		ai_will_do = { 
			base = 1
			modifier = {
				条件
				add/factor = X
			}
		}
		available = {
			国策可点的条件
		}
		bypass = {
			跳过国策的条件
		}
		
		search_filters = { 标签 }

		completion_reward = {	
			效果
		}
	}
	focus = {
		id = 单独国策ID（或名字）
		icon = 在.gfx文件里的图片名
		relative_position_id = 相对国策ID
		prerequisite = { focus = 第一个前提国策ID } 
		prerequisite = { focus = 第二个前提国策ID } 
		prerequisite = { focus = 第N个前提国策ID } 先决国策，需要点完所有的prerequisite的国策，实线
		mutually_exclusive = { focus = 互斥国策ID }
		x = 以相对国策为原点的X轴横向坐标
		y = 以相对位国策为原点的Y轴纵向坐标

		cost = 国策需要的天数，一个值等于7天，可加小数点

		ai_will_do = { 
			base = 1
			modifier = {
				条件
				add/factor = X
			}
		}
		available = {
			国策可点的条件
		}
		bypass = {
			跳过国策的条件
		}
		
		search_filters = { 标签 }

		completion_reward = {	
			效果
		}
	}
	focus = {
		id = 单独国策ID（或名字）
		icon = 在.gfx文件里的图片名
		relative_position_id = 相对国策ID
		prerequisite = { focus = 前提国策ID }
		mutually_exclusive = { focus = 互斥国策ID }
		x = 以相对国策为原点的X轴横向坐标
		y = 以相对位国策为原点的Y轴纵向坐标

		available_if_capitulated = yes/no 投降后是否能点
		cancel_if_invalid = yes/no 条件不满足是否取消
		continue_if_invalid = yes/no 条件不满足是否继续
		will_lead_to_war_with = TAG 正在向哪个国家制造战争借口

		allow_branch = { 条件 } 满足什么条件可以显示这个国策及以下的国策树，配合mark_focus_tree_layout_dirty = yes效果刷新国策树

		cost = 国策需要的天数，一个值等于7天，可加小数点

		ai_will_do = { 
			base = 1
			modifier = {
				条件
				add/factor = X
			}
		}

		bypass = {
			跳过国策的条件
		}
		
		search_filters = { 标签 }

		completion_reward = {	
			效果
		}
	}
	
}